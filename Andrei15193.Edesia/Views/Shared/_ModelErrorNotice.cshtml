@model System.Web.Mvc.ModelState
@if (Model != null && Model.Errors != null && Model.Errors.Count > 0)
{
	<ul class="margin5">
		@foreach (ModelError modelError in Model.Errors)
		{
			<li class="no-margin no-padding fg-red">
				@modelError.ErrorMessage
			</li>
		}
	</ul>
}